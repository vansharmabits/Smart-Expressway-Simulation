#include "../include/Expressway.h"

void runCLI(Expressway& e) {
    while(true) {
        cout << "\n1. Shortest Path\n2. MST\n3. Exit\n";
        int ch; cin >> ch;

        if(ch == 3) break;
    }
}